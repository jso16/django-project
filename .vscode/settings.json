{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:django_project}/db.sqlite3",
            "name": "db_connection"
        }
    ]
}